export const branchesSelectLabelFormat = (items: string[]) => `${items.length} branches selected`
export const metricsSelectLabelFormat = (items: string[]) => `${items.length} metrics selected`
export const testsSelectLabelFormat = (items: string[]) => `${items.length} tests selected`
export const modeSelectLabelFormat = (items: string[]) => `${items.length} modes selected`
