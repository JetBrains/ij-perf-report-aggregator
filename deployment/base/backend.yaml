apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  labels: &LABELS
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels: *LABELS
  template:
    metadata:
      labels: *LABELS
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: backend
          image: ko://github.com/JetBrains/ij-perf-report-aggregator/cmd/server
          args: ["--db", "clickhouse:9000", "--nats", "nats://nats:4222"]
          env:
            - name: SERVER_PORT
              value: "8080"
          resources:
            requests:
              memory: 256Mi
              cpu: "0.4"
            limits:
              cpu: "0.9"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health-check
              port: 8080
          livenessProbe:
            httpGet:
              path: /health-check
              port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: backend